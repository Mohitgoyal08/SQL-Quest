import React from 'react';

/**
 * Architectural placeholder for future multi-island URL routing.
 * Currently delegates directly to our state-machine shell in App.jsx.
 */
export default function GameRoutes({ children }) {
  return <>{children}</>;
}